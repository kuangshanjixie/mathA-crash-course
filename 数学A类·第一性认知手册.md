# 数学 A 类 · 第一性认知手册

> 写给一个**已经学过大学高数、线代、离散、概率**、但不是数学专业的同学。
> 目标:看完这一份,你能**听懂**数 A 卷子上每一句话在说什么、它属于考纲哪一块、
> 你**还差哪些拼图**才有可能从"看天书"走到"能动手"。
> 这不是解题手册,是**术语扫盲 + 全景地图**。第一性接触,先建立感性认知。

---

## 目录

- [Part 0 宏观认知篇 — 先看清整张卷子和这条赛道](#part-0-宏观认知篇--先看清整张卷子和这条赛道)
- [Part 1 数学分析篇](#part-1-数学分析篇)
- [Part 2 高等代数篇](#part-2-高等代数篇)
- [Part 3 解析几何篇](#part-3-解析几何篇)
- [Part 4 决赛扩展篇澄清 — 你看到的"超纲题"真相](#part-4-决赛扩展篇澄清--你看到的超纲题真相)
- [Part 5 收尾 — 补短板路线图与心态](#part-5-收尾--补短板路线图与心态)

问题排序逻辑:**先把"卷子长什么样、黑话怎么读"讲清(Part 0)→ 三大块按考纲权重数分→高代→解几(Part 1-3)→ 你记录里那些其实不在初赛范围里的"天书题"统一澄清(Part 4)→ 补短板路线(Part 5)**。每一块内部,把"同一个家族的概念"聚在一起讲,避免东一榔头西一棒。

---

## Part 0 宏观认知篇 — 先看清整张卷子和这条赛道

### 0.1 一句话定位

> 全国大学生数学竞赛 **数学专业 A 类初赛** = 一张 6 大题、100 分、150 分钟的纯证明/推导卷,
> 考 **数学分析(50%)+ 高等代数(35%)+ 解析几何(15%)** 三门数学系基础课。
> 它**不是**你大学学的"高数+线代"的加难版,而是**数学系**的同一门课的进阶版——
> 同名,但**深度、严格性、证明量**完全不在一个量级。

把这个定位刻在脑子里,后面所有的"为什么这么难""为什么我听不懂"都有答案了。

### 0.2 卷子结构:6 题 / 100 分 / 150 分钟

```
┌──────┬──────┬──────┬──────┬──────┬──────┬───────┐
│ 题号 │  一  │  二  │  三  │  四  │  五  │  六   │
├──────┼──────┼──────┼──────┼──────┼──────┼───────┤
│ 分值 │  15  │  15  │  20  │  15  │  15  │  20   │
└──────┴──────┴──────┴──────┴──────┴──────┴───────┘
        合计 100 分    考试时长 150 分钟(2.5 小时)
```

**考点排列的典型规律**(看 7 套真题归纳,不是铁律):

```
题1(15) 偏 解析几何      ──  曲面/切平面/旋转面,送分但要快
题2(15) 偏 数学分析(轻)  ──  求极限/含参积分/级数判敛
题3(20) 偏 数学分析(重)  ──  综合证明,中值定理/Taylor/积分不等式
题4(15) 偏 高等代数(轻)  ──  矩阵/行列式/特征值计算
题5(15) 偏 高等代数(重)  ──  线性空间/线性变换/二次型证明
题6(20) 综合/较大计算    ──  常是数分或代数的硬骨头
```

> 注意:这是**归纳出来的常见 pattern**,不是官方钦定。具体每届会有错位,
> 但**三大块的分值比例 50/35/15 是官方锁死的**。

### 0.3 风格扫盲:全是证明大题,没有选择填空

这一点对你心态影响最大,先说清楚:

```
你熟悉的(高数/线代期末、考研数一):        数 A 的实际样子:
┌────────────────────────────┐         ┌────────────────────────────┐
│ 选择 4 选 1                  │         │ (没有)                      │
│ 填空 写个数                  │         │ (没有)                      │
│ 大题 计算 + 简单证明          │         │ 6 道纯证明/推导大题          │
│ 答案对就满分                  │         │ 分步给分,过程即分数         │
└────────────────────────────┘         └────────────────────────────┘
```

卷面上你会看到的步骤分标记(真题里就长这样):

```
   ........................................................   (4 分)
   ........................................................   (7 分)
   ........................................................   (10 分)
   ........................................................   (15 分)
```

意思是:**这道题分成若干步骤,每一步给你标了分**。你写不到最后,前面步骤对了也能拿分;
反过来,**跳步、不写理由、直接给结论,即使答案对也拿不到几分**。

> 这一条决定了你的训练方向:**练"把每一步为什么讲清楚"**,而不是练"算得快"。

### 0.4 "题不多但每题都很重" — 对比蓝桥

你说"感觉比蓝桥难""真的做得完吗",这两句话都对,但要拆开看:

```
                  蓝桥(编程竞赛)              数 A(数学竞赛)
题量              6~8 题,每题多个小问         6 题,每题 1~3 小问
单题耗时          30 分钟 ~ 1 小时             20 ~ 30 分钟(理想)
能力核心          算法+实现+调试               严格证明+计算+推理
"做完"的标准      AC 几道                      6 题都写出完整证明
满分可能性        极少                         极少
拿奖门槛          答对一定数量                  按总分排名,分高者进决赛

→ 数 A 的 "做完" 不是"6 题都会",而是 "能拿到的分都拿到"。
→ 150 分钟拿满 100 分几乎是神仙; provincial 一等奖常常 50~70 分就够。
→ 你的对手不是卷子,是同省其他选手的分数分布。
```

所以**问"做得完吗"问错了问题**。正确的问题是:
**"150 分钟里,我能稳定拿下的分是多少?哪些题该果断放弃?"**

这也是为什么 Part 5 会强调:训练目标是**提高稳定输出**,不是"全会"。

### 0.5 黑话词典(集中讲,后面 Part 不再重复)

你被"黑话"卡住,核心原因是**这些词的英文原文你其实认识,只是中译让你觉得玄**。
下面这张表是整份手册里最重要的一张,反复回来查。

#### 0.5.1 行文黑话

| 中文黑话 | 英文原文 | 大白话翻译 | 出现时该怎么反应 |
|---|---|---|---|
| 不失一般性 | Without loss of generality (WLOG / w.l.o.g.) | "我们可以只讨论一种情况,其他情况完全同理" | 他在**简化场景**,接下来只讲一个情形,你要信其他情形证明一样 |
| 设 / 令 / 记 | Let / Denote / Set | "以下我们引入一个记号" | 后面要用这个符号了,记住定义 |
| 记为 / 即 | i.e. (id est) | "也就是说" | 换个写法重述 |
| 亦即 | that is / viz. | 同上 | 同上 |
| 充分性 / 必要性 | sufficiency / necessity | "⇒ 这方向" / "⇐ 这方向" | 证明 A⇒B 分两半:充分性是证 A⇒B,必要性是证 B⇒A |
| 显然 / 易证 | obviously / easily shown | (经常是偷懒) | 别信,自己验一遍,有时是个坑 |
| 注意到 | note that / observe that | "我提醒你一个关键事实" | **重点提示来了**,这句后面 usually 是解题钥匙 |
| 从而 / 故 / 故而 | hence / therefore | "所以" | 推进下一步 |
| 反之 | conversely | "反过来也对" | 要开始证反方向 |
| 若不然 / 假设相反 | suppose not / assume the contrary | 反证法开局 | 接下来要推出矛盾 |
| Q.E.d. / □ | quod erat demonstrandum | "证毕" | 这段证明结束 |

> **"不失一般性" = WLOG** 是你最该记住的一个。把它脑内翻译成
> "只讲一种情况,其他同理",神秘感立刻消失。

#### 0.5.2 符号黑话(下面 Part 还会展开,这里先列总表)

| 符号 | 读法 | 含义 | 你最容易在哪见到 |
|---|---|---|---|
| $\forall$ | for all / 任意 | 对所有 | 任意取定 |
| $\exists$ | exists / 存在 | 存在(至少一个) | 找到一个就够 |
| $\in$ | belongs to / 属于 | 是…的元素 | $x\in\mathbb{R}$ |
| $\subset$ / $\subseteq$ | subset / 包含于 | 是…的子集 | 子空间 |
| $\Rightarrow$ | implies / 推出 | 推出 | 条件→结论 |
| $\Leftrightarrow$ / $\iff$ | iff / 当且仅当 | 等价 | 两向都成立 |
| $\partial B$ | boundary of B / B 的边界 | B 的边界集 | 见 Part 1.3 |
| $\ker T$ | kernel of T / T 的核 | $T$ 的核(零空间) | 见 Part 2.7 |
| $\mathrm{Im}\,T$ / $T(V)$ | image of T / T 的像 | $T$ 的值域 | 线性映射 |
| $\oplus$ | direct sum / 直和 | 直和分解 | 见 Part 2.11 |
| $\otimes$ | tensor product / 张量积 | 张量积 | 出现就意味着高阶专题 |
| $\mathbb{R}$ / $\mathbb{C}$ / $\mathbb{Q}$ / $\mathbb{Z}$ / $\mathbb{N}$ | 实/复/有理/整数/自然数域 | 数集 | 几乎每题 |
| $\mathbb{C}^n$ | C-n | $\mathbb{C}$ 上 $n$ 维列向量全体 | 见 Part 2.1 |
| $\mathbb{R}^{n\times n}$ / $M_n(\mathbb{C})$ | $n$ 阶方阵全体 | $n\times n$ 矩阵集合 | 高代首句 |
| $A^T$ / $A^*$ / $A^{-1}$ | 转置 / 共轭转置 / 逆 | 三种矩阵操作 | 高代 |
| $\mathrm{tr}\,A$ | trace / 迹 | 对角元之和 | 特征值之和 |
| $\det A$ / $|A|$ | determinant / 行列式 | 行列式 | 高代 |
| $\mathrm{rank}\,A$ | rank / 秩 | 秩 | 高代 |
| $\square$ / $\blacksquare$ | 证毕 | 证明完结 | 每段证明末尾 |

> **圆圈中间一个十字 = $\oplus$,读"直和"**,直和分解。
> 圆圈中间一个**叉** = $\otimes$,读"张量积",更高级,初赛基本不考。
> 你问的"圆圈十字"就是 $\oplus$,见 Part 2.11 展开。

#### 0.5.3 "xxx 均有意义"是什么黑话?

出现"矩阵 $A$ 使 $\arctan A$ 有意义""$\log A$ 有意义""$\sin A$ 有意义"时,
"有意义"的精确含义是:

> **这个矩阵函数能被良好定义**——通常指用幂级数定义时级数收敛,
> 或用 Jordan 标准形定义时定义式在每个 Jordan 块上都成立。

正常 $\arctan x = \sum_{k=0}^{\infty} \frac{(-1)^k}{2k+1} x^{2k+1}$ 是给**数**用的。
要让 $\arctan A$ 对**矩阵**有意义,需要这个幂级数在矩阵意义下收敛,
这要求 $A$ 的所有特征值落在 $\arctan$ 幂级数的收敛域内。

所以"使 $\arctan A$ 有意义"翻译成可执行条件往往是:
**$A$ 的谱(特征值集合)满足某种范围/约束**。详见 Part 2.10。

### 0.6 纯论证存在吗? — 是的,而且是大头

你以为"数学卷=一堆公式",其实数 A 的卷子是**用文字论证串起来的**,
公式只是论证的"零件"。一道典型证明题长这样:

```
设 f 在 [a,b] 上连续,在 (a,b) 内可导,且 f(a)=f(b)=0.
证明:存在 ξ∈(a,b),使得 f'(ξ)=f(ξ).
─────────────────────────────────────────────────────────
证明:令 F(x)=e^{-x}f(x).                         ← 构造辅助函数
   由 f 连续、e^{-x} 连续,知 F 在 [a,b] 连续,    ← 这一行是文字论证
   且在 (a,b) 可导.                              ← 还是文字论证
   又 F(a)=e^{-a}f(a)=0, F(b)=e^{-b}f(b)=0,      ← 计算
   由 Rolle 中值定理,∃ξ∈(a,b),F'(ξ)=0.          ← 调用定理
   而 F'(x)=e^{-x}(f'(x)-f(x)),                  ← 求导
   故 f'(ξ)-f(ξ)=0,即 f'(ξ)=f(ξ).  □            ← 收尾
```

看比例:**文字论证行 ≥ 公式行**。所以训练时要去练"每一步给理由"。
你大学高数里那种"写个公式就给分"的题,在数 A 里基本拿不到分。

### 0.7 非数学专业的你,短板在哪

一张图说清楚(✓=你大概有,△=有但不够深,✗=大概率没学过):

```
知识块                  高数/线代/离散/概率    数 A 需要         差距
─────────────────────────────────────────────────────────────────
极限/连续/微分          ✓                      ✓✓(严格ε-δ)     △  严格性
Riemann 积分            ✓(会算)               ✓(定义+判定)     △  可积性判定
反常积分/含参积分       △(略提)              ✓✓                ✗  几乎全新
级数(数项/函数项/一致) △                      ✓✓                ✗  一致收敛
多元积分+场论三大定理   ✓                      ✓                △  证明+换元
─────────────────────────────────────────────────────────────────
行列式/矩阵/方程组      ✓                      ✓                △  结构化技巧
特征值/对角化           ✓(到这)               ✓✓                △  往下还多
线性空间/线性映射/商空间 ✓(浅)                ✓✓                ✗  抽象层
二次型/合同/正定        △                      ✓                ✗  几何+代数联系
多项式(整除/不可约)     △(离散里)            ✓                ✗  数域上分解
Cayley-Hamilton/最小多项式 ✗                  ✓                ✗  全新
─────────────────────────────────────────────────────────────────
向量/平面/直线          ✓                      ✓                ✓  基本够
二次曲线/曲面           △                      ✓                △  分类+标准形
旋转面/切面/法截线      ✗                      ✓                ✗  新
脐点/曲率/挠率          ✗                      △(解几里浅)     ✗  微分几何味道
坐标变换/旋转平移       △                      ✓                △
─────────────────────────────────────────────────────────────────
群/环/域/理想           △(离散里浅)          【初赛 A 不考】   —  见 Part 4
概率+矩阵               ✓                      【初赛 A 不考】   —  见 Part 4
```

**一句话总结你的短板**:
> 你算得动,但**严格证明的语言没建立**、**线性空间/多项式这两块的抽象层没建过**、
> **级数与含参积分几乎全新**、**解几里"几何味"专题没接触**。
> 这四块就是你和数 A 之间的鸿沟。后面 Part 1-3 会逐块讲 What & Why,
> Part 5 给出补齐顺序。

---

## Part 1 数学分析篇

这一部分回答你三个问题:
- **1.1** 上确界 / 下确界 — 这是非数学专业的数吗?高数学过吗?
- **1.2** Riemann 可积到底是什么?为什么还有"别的"积分定义?真有这么复杂?
- **1.3** $x\in\partial B$ 是什么?边界怎么"翻译成可执行条件"放进推导?

### 1.1 上确界 / 下确界(sup / inf)

#### 它在不在你的高数里?

**基本不在**。你的《高等数学》教材多半只在"数列极限"里轻描淡写提一句,
没有正式建立"确界理论"。而**数学分析第一章**就会正式讲它,因为它是
**实数连续性六定理**这一族的核心成员——这族定理是整个数分严格性的地基:

```
实数连续性六定理(等价,记住一组就够):
┌─────────────────────────────────────────────────────────┐
│ 1. 确界存在定理   非空有上界的数集必有上确界              │
│ 2. 单调有界定理   单调有界数列必收敛                      │
│ 3. 区间套定理     闭区间套交非空且唯一点                  │
│ 4. Bolzano–W.     有界数列必有收敛子列                     │
│ 5. Cauchy 收敛    Cauchy 列必收敛(完备性)                │
│ 6. 有限覆盖(H-B) 闭区间开覆盖有有限子覆盖(紧致性)      │
└─────────────────────────────────────────────────────────┘
        ↓ 都等价于"实数轴没有洞"
```

> 数 A 卷子上,这族定理是**证明题的隐形主骨架**——很多"存在 ξ 使得…"的题,
> 本质就是套上面某条定理。

#### 定义

设 $E\subset\mathbb{R}$ 非空有上界。$M=\sup E$ 读作"$E$ 的上确界",定义为:

$$
\sup E = M \iff
\begin{cases}
\forall x\in E,\ x\le M & \text{(是上界)}\\[2pt]
\forall \varepsilon>0,\ \exists x\in E,\ x>M-\varepsilon & \text{(是最小上界,任何下调都被戳破)}
\end{cases}
$$

对称地,$m=\inf E$ 是**下确界**(最大下界)。直觉图:

```
   数轴 ────────────────────────────────────→
            ┌── E 的元素们 ──┐
        ●  ●  ●  ●  ●  ●  ●  ●   ·····
                                     ↑
                                    sup E
                                    (可能不属于 E!)

   sup 的两副面孔:
   ① 它罩得住 E        :  ∀x∈E, x ≤ sup E
   ② 但压不下去一点点  :  任意往下挪 ε,就有 E 中元素戳上来
```

#### 关键陷阱:sup ≠ max

| | $\max E$ | $\sup E$ |
|---|---|---|
| 必须属于 $E$? | **必须** | **不必** |
| $E=(0,1)$ | 不存在 | $=1$ |
| $E=[0,1]$ | $=1$ | $=1$ |
| $E=\{1-1/n:n\ge1\}$ | 不存在(极限点 1 不在集里) | $=1$ |

> 数 A 中**几乎所有"取 sup"的步骤都要警惕sup 是否属于集合**——它决定了
> 你能不能说"存在 $x_0\in E$ 使 $x_0=\sup E$"。这一句话写错,后面整段可能崩。

#### 出现频率

极高。典型场景:
- 一致连续 / 一致收敛的 $\sup$ 刻画:$f$ 一致连续 $\iff \lim_{\delta\to0}\sup_{|x-y|<\delta}|f(x)-f(y)|=0$。
- 积分上下和:$U(P,f)=\sum M_i\Delta x_i$,其中 $M_i=\sup_{[x_{i-1},x_i]} f$。
- 证明不等式:$\sup\{a_n\}\le M$ 一句话 = $\forall n, a_n\le M$。

### 1.2 Riemann 可积是什么 + 其他积分定义

#### 先承认一个事实

你的高数大概率只教了"**怎么算**定积分"(牛顿–莱布尼茨公式 $\int_a^b f = F(b)-F(a)$),
**没正式讲"什么叫可积"**。数 A 要的不是算,是**判定 + 证明可积性 + 用定义推性质**。

#### Riemann 积分定义(标准三步)

```
第①步  分割      把 [a,b] 切成 n 段
                   P : a = x₀ < x₁ < ⋯ < xₙ = b
                   记 Δxᵢ = xᵢ − x_{i−1},  ‖P‖ = max Δxᵢ (分割细度)

第②步  取样本点   每段随便挑一点 ξᵢ ∈ [x_{i−1}, xᵢ]
                   做黎曼和  S(P, f) = Σᵢ f(ξᵢ)·Δxᵢ

第③步  取极限     让 ‖P‖ → 0
                   若 S(P, f) → 一个固定数 I
                   且【不论怎么分割、怎么取样本点】都→ I
                   则称 f 在 [a,b] 上 Riemann 可积,记 ∫ₐᵇ f = I
```

#### 等价的 Darboux 形式(判定更好用)

用**上和 / 下和**(不再依赖样本点):

$$
U(P,f)=\sum_{i} M_i\,\Delta x_i,\quad M_i=\sup_{[x_{i-1},x_i]} f
$$
$$
L(P,f)=\sum_{i} m_i\,\Delta x_i,\quad m_i=\inf_{[x_{i-1},x_i]} f
$$

```
    │  f
    │      ╱╲            U(P,f): 每段取 sup → 上台阶包住 f
    │   ╱╲╱  ╲           L(P,f): 每段取 inf → 下台阶托住 f
    │  ╱      ╲
    │ ╱     ───╲──        f 夹在 U 和 L 之间
    │╱            ╲
    └──────────────→ x
    a            b

   可积 ⟺ 上积分 = 下积分
        ⟺ inf_P U(P,f) = sup_P L(P,f)
        ⟺ ∀ε>0, 存在分割 P 使 U(P,f) − L(P,f) < ε
```

#### 三个最常用的可积判定(背下来)

```
f 在 [a,b] 上 Riemann 可积,只要满足下列【任一】条件:
 ① f 在 [a,b] 上连续                         ← 最常用
 ② f 在 [a,b] 上有界,且仅有有限个间断点
 ③ f 在 [a,b] 上单调
```

> 还有更深的判据(Lebesgue:"有界 + 间断点集测度为 0"),但属于实变函数内容,
> **初赛 A 不要求**,决赛高年级选做实变才用。

#### 反例(让你看清"可积"这件事真的有内容)

| 函数 | 在 $[0,1]$ 上 | 为什么 |
|---|---|---|
| $f(x)=x$ | 可积 | 连续 |
| $\mathrm{sgn}(x)$ 在 $[-1,1]$ | 可积 | 仅 1 个间断点(有限) |
| Dirichlet $D(x)=\begin{cases}1,&x\in\mathbb{Q}\\0,&x\notin\mathbb{Q}\end{cases}$ | **不可积** | 处处间断,上下和恒差 1 |
| $f(x)=1/x$ 在 $(0,1]$ | 不是 Riemann 积分的事 | **无界**,要先做反常积分 |

#### "其他积分定义"全景图

你说"还有其他积分定义的考法吗?这么复杂?"——是的,但**初赛 A 只圈定其中一部分**。
下表把"积分家族"列清楚,标出哪些考哪些不考:

```
┌─────────────────────────┬────────┬──────────────────────────────────┐
│ 积分类型                │ 初赛 A │ 说明                              │
├─────────────────────────┼────────┼──────────────────────────────────┤
│ Riemann 积分             │  ★必考 │ 数分核心,定义+判定+性质         │
│ 反常积分(无穷区间/无界) │  ★必考 │ 不是新定义,是 Riemann+极限       │
│ 含参变量积分 / 含参反常  │  ★必考 │ 一致收敛判别是 favorite          │
│ Riemann–Stieltjes 积分  │  △偶考 │ Σf(ξᵢ)(g(xᵢ)−g(x_{i−1}))         │
│ 第一/二型线积分、面积分  │  ★必考 │ 多元部分,配 Green/Gauss/Stokes  │
├─────────────────────────┼────────┼──────────────────────────────────┤
│ Lebesgue 积分            │  ✗不考 │ 实变函数,决赛高年级选做          │
│ (测度论那套)             │        │                                  │
└─────────────────────────┴────────┴──────────────────────────────────┘
```

**结论**:你不需要被"勒贝格/斯蒂尔杰斯/…"吓到。初赛 A 圈定的就是
**Riemann 体系 + 反常 + 含参 + 多元线面积分**,只要把这五块的可积性判定和
一致收敛玩熟,数分积分部分就稳了。

### 1.3 边界 $\partial B$ — 是什么、怎么"翻译"成可执行条件

#### 先破一个误会

$\partial$ 这个符号在两个地方出现,含义**完全不同**,别混:

```
   ∂f/∂x   ←  偏导数(partial derivative),读 "partial"
   ∂B      ←  B 的边界(boundary),读 "boundary of B"
```

字形一样,语境不同。在 $x\in\partial B$ 这种地方,**必是"边界"**。

#### 定义(在 $\mathbb{R}^n$ 里)

$$
\partial B \;=\; \overline{B}\setminus B^\circ
$$

即**闭包减内部**。等价的"双邻域"刻画(更实用):

$$
x\in\partial B \;\iff\; \forall\varepsilon>0,\ \big(U(x,\varepsilon)\cap B\neq\varnothing\big)\ \text{且}\ \big(U(x,\varepsilon)\cap B^c\neq\varnothing\big)
$$

> 翻译:$x$ 的**任意**邻域里**同时**有 $B$ 的点、也有 $B$ 外面的点。
> 也就是 $x$ "一脚踩在 $B$ 里、一脚踩在 $B$ 外"。

#### 直觉图

一维:
```
   ℝ 数轴
   ──────●━━━━━━━━●──────→ x
         0        1
         ↑        ↑
        ∂B       ∂B      B=(0,1) 的边界 = {0, 1}
                       (内部 B°=(0,1), 闭包 B̄=[0,1])
```

二维(单位开圆盘):
```
            y
            ↑
       ╭────●────╮        ●  边界 ∂B  = 单位圆周 {x²+y²=1}
      │  ○ ○ ○   │       ○  内部 B°  = 开圆盘 {x²+y²<1}
      │ ○     ○  │
      │  ○ ○ ○   │       闭包 B̄ = B° ∪ ∂B = 闭圆盘 {x²+y²≤1}
       ╰─────────╯
            └──────→ x
```

#### "看到 $x\in\partial B$,我能换成哪些等价写法?"

这是你问的关键。下表是**翻译工具箱**,在推导里随时替换:

| 看到 | 可以替换成 | 什么时候用 |
|---|---|---|
| $x\in\partial B$ | $x\in\overline{B}$ 且 $x\notin B^\circ$ | 想用闭包 / 内部分别处理时 |
| $x\in\partial B$ | $\exists\{x_n\}\subset B,\ x_n\to x$ 且 $\exists\{y_n\}\subset B^c,\ y_n\to x$ | 想用序列逼近时 |
| $x\in\partial B$ | $\forall\varepsilon>0,\ U(x,\varepsilon)\cap B\ne\varnothing$ 且 $U(x,\varepsilon)\cap B^c\ne\varnothing$ | 想"任意邻域"论证时 |
| $x\in\partial B$ (若 $B$ 闭) | $x\in B$(因为闭集 $\overline B=B$,$\partial B\subset B$) | $B$ 是闭区域时,边界点也在 $B$ 里 |
| $x\in\partial B$ (若 $B$ 开) | $x\notin B$ | $B$ 开时,边界点不属于 $B$ |

> **最常考的应用**:Green / Gauss / Stokes 公式里,积分区域写成 $D$ 或 $\Sigma$,
> 而**沿边界积分**就写成 $\oint_{\partial D}$、$\iint_{\partial\Omega}$ 等。
> "$\partial$"在多元积分里**永远指边界**。

#### 一个完整的"翻译 → 推导"小例子

> 命题:设 $B\subset\mathbb{R}^n$ 是**开**集,$f:\overline{B}\to\mathbb{R}$ 连续,
> 且 $f$ 在 $\partial B$ 上恒为 $0$。求证:$f$ 在 $\overline{B}$ 上有最大最小值。

思路(看怎么把 $\partial B$ 翻译成条件):

```
1. f 在闭包 B̄ 连续  →  若 B̄ 有界闭(=紧),由最值定理,f 在 B̄ 上取到最值。 ✓
2. 但题目没说 B 有界! ⚠ 这里要警惕
3. ∂B 上 f=0 这个条件,翻译:
      "x∈∂B ⟹ f(x)=0"
   即边界是 f 的零点集的超集。
4. 配合连续性,可推出 f 在 B 内的行为被边界值"压住"……
```

(这只是示意"翻译"动作,完整证明要看具体条件。重点是:**看到 $\partial B$
先反应"我用哪种等价刻画最顺手"**,而不是卡在符号上。)

---

## Part 2 高等代数篇

这一部分回答你**最多**的问题。先列清单,你可以一个个对照:
- 2.1 $n$ 阶**复方阵**是什么? $x\in\mathbb{C}^n$ 是什么书写,能写出什么公式?
- 2.2 线代"学到特征根就没了"——**后面还有啥**?前面有"竞赛技巧"吗?
- 2.3 特征多项式及相关: $A^T$、$A^*$、$p(A)$ 都是什么?
- 2.4 **几何重数** vs 代数重数(易混)
- 2.5 特殊矩阵**全家桶**(酉矩阵、Hermite、正交、正规、幂零、幂等、正定…)
- 2.6 正定 / 负定二次型怎么定义、能推出什么
- 2.7 $\ker$ 是什么(核)
- 2.8 $\oplus$(圆圈十字)直和符号
- 2.9 线性算子的考点与符号
- 2.10 "$\arctan A$ 有意义"这类黑话——矩阵函数
- 2.11 矩阵与极限结合的考法
- 2.12 高代前面的竞赛技巧(行列式 / 方程组 / 分块)

### 2.1 $n$ 阶复方阵 & $x\in\mathbb{C}^n$

#### 复方阵

$n$ 阶**复方阵** = **所有元素都是复数**的 $n\times n$ 方阵,全体记作

$$
A\in M_n(\mathbb{C})\quad\text{或}\quad A\in\mathbb{C}^{n\times n}
$$

> 关键是"$\mathbb{C}$"——意味着 $a_{ij}\in\mathbb{C}$,可以带虚数单位 $i$。
> 这件事比你以为的重要:在**复数域**上,特征多项式一定能完全分解(代数基本定理),
> 所以特征值永远存在;而**实方阵**在 $\mathbb{R}$ 上可能没有实特征值
> (例:$\begin{pmatrix}0&-1\\1&0\end{pmatrix}$ 特征是 $\pm i$)。
> 数 A 高代默认在 $\mathbb{C}$ 上讨论,除非特别声明"$\mathbb{R}$ 上"。

#### $x\in\mathbb{C}^n$ 这个写法

$\mathbb{C}^n$ = "所有 $n$ 个复数组成的列向量"的集合:

$$
x\in\mathbb{C}^n \;\iff\; x=(x_1,x_2,\dots,x_n)^{T},\quad x_k\in\mathbb{C}
$$

**"看到 $x\in\mathbb{C}^n$" 你能立刻写出哪些公式**——这是你问的核心。下面这张表
是"$\mathbb{C}^n$ 默认配齐的一整套":

```
$x\in\mathbb{C}^n$ 意味着默认有这些东西可用:
┌───────────────────────────────────────────────────────────────┐
│ ① 分量写法    x = (x₁,…,xₙ)ᵀ,  xₖ ∈ ℂ                         │
│ ② 范数        ‖x‖ = √(Σ|xₖ|²) = √(x* x)  (注意 |·| 取模)      │
│ ③ 内积        ⟨x,y⟩ = Σ xₖ conj(yₖ) = y* x  (复内积!)          │
│ ④ 共轭转置    x* = conj(x)ᵀ  (一个 1×n 行向量)                 │
│ ⑤ 加法、数乘  x+y,  αx,  α∈ℂ                                  │
│ ⑥ 它是个线性空间 维数 = n,基可取 e₁,…,eₙ                       │
└───────────────────────────────────────────────────────────────┘
```

> ★ 你最容易栽的地方:**复内积"共轭在后"** $\langle x,y\rangle=\sum x_k\overline{y_k}$,
> 不是 $\sum x_k y_k$。在 $\mathbb{R}^n$ 里共轭无所谓,在 $\mathbb{C}^n$ 里少了共轭,
> $\langle x,x\rangle$ 可能变负、甚至复数,范数都崩了。

#### 一个标准化的"翻译"示例

> 题面:"设 $A\in M_n(\mathbb{C})$, $x\in\mathbb{C}^n$, $x\ne0$, $Ax=\lambda x$。"

你脑内翻译成:

```
A 是 n×n 复矩阵;
x 是 n 维复列向量,不是零向量;
存在复数 λ 使  A·x = λ·x   (这是特征值定义!)
→ λ 是 A 的特征值, x 是对应特征向量
→ 进一步可写 (λI − A)x = 0,  x ∈ ker(λI − A) \ {0}
→ λI − A 奇异 ⟹ det(λI − A) = 0
```

每一行都是你后面推证的可用弹药。**"$x\in\mathbb{C}^n$"从不是装饰,
它一次性给了你上面 6 件武器**。

#### 一张"数集字母对照"

你在卷子上会反复看到,务必刻在脑里:

| 字母 | 集合 | 典型出现 |
|---|---|---|
| $\mathbb{N}$ | 自然数 | $n\in\mathbb{N}$ |
| $\mathbb{Z}$ | 整数 | $k\in\mathbb{Z}$ |
| $\mathbb{Q}$ | 有理数 | 多项式在有理数域上分解 |
| $\mathbb{R}$ | 实数 | $\mathbb{R}^n$ 欧氏空间 |
| $\mathbb{C}$ | 复数 | $\mathbb{C}^n$, $M_n(\mathbb{C})$ |

> 一个**关键考点**:$\mathbb{Q}$ 上的多项式不一定能分解(如 $x^2-2$),
> $\mathbb{R}$ 上的多项式最多分解到二次(如 $x^2+1$),
> $\mathbb{C}$ 上的多项式一定完全分解为一次(代数基本定理)。
> 多项式题里**数域**这个限定词决定了你能不能"提根"。

### 2.2 线代"学到特征根就没了"——后面还有啥?前面有竞赛技巧吗?

你说"线代好像学到特征根就没了"——这正是非数学专业和数学系的**分水岭**。
下面这张地图让你看清**全貌**,你的大学线代大概只覆盖了前 4 列。

#### 高代全景地图(数A考点分布)

```
                  ┌─ 你大学线代大概率覆盖 ─┐   ┌─ 数A要但你没学 ─┐
                  ▼                         ▼

第1层 行列式        ■ 性质/展开/初等变换        ■ Vandermonde、分块、递推技巧
第2层 矩阵运算      ■ 秩、逆、初等矩阵          ■ 秩不等式、Schur补、分块技巧
第3层 线性方程组    ■ 解空间、秩-零度           ■ AX=B 结构化处理
第4层 线性空间      △ 基/维数(浅)              ■ 子空间、和、直和、商空间 ★
第5层 线性映射      △ 像与核(浅)              ■ 表示矩阵、不变子空间、商映射 ★
第6层 特征值/对角化 ■ 特征根/可对角化          ■ 同时对角化、可交换族 ★
─── 你停在这里 ──────────────────────────────────────────────────
第7层 Cayley-Hamilton          ✗ 全新: f(A)=0 的威力,A⁻¹=p(A) ★
第8章 最小多项式               ✗ 全新: 区分重数的核心工具 ★
第9层 Jordan 标准形            ✗ 全新: 不可对角化时的"标准像" ★★
第10层 λ-矩阵 / 初等因子       ✗ 全新: Jordan 形的构造理论(可能跳过)
第11层 二次型 / 合同           △ 浅: 标准形  ✗ 深: 正定判别、惯性指数、Sylvester ★
第12章 多项式                  △ 离散里浅  ✗ 数域上整除/不可约/根与系数 ★
第13层 内积空间 / 酉           △ 实内积     ✗ 复内积、酉矩阵、正规矩阵、Schur分解 ★★
第14层 矩阵函数 / 矩阵分析     ✗ 全新: eᴬ, arctanA, 极限, 谱半径 △(偶考)
```

> △=浅,■=够,✗=没学过。**★ 越多越常考**。
> 你的短板集中在 **4-5 层的抽象层 + 7-9 层的 canonical form + 11-13 层**。
> 这就是"特征根之后"的整个后半本高代。

#### "前面也有竞赛技巧吗"——有,而且很重

| 层 | 典型技巧 |
|---|---|
| 行列式 | Vandermonde 化、加边法、拆分法、递推降阶、用 $AB$ 的行列式 = 积 |
| 矩阵 | 秩不等式 $\mathrm{rank}(A+B)\le r(A)+r(B)$、Sylvester $r(AB)\ge r(A)+r(B)-n$、分块行列式 $\det\begin{pmatrix}A&B\\0&D\end{pmatrix}=\det A\det D$、Schur 补 |
| 方程组 | $Ax=0$ 解空间维数 $=n-r(A)$、把解当不变子空间处理 |
| 多项式 | $f(x)=q(x)g(x)+r(x)$、Eisenstein 判别、有理根定理、Vieta 公式 |

> 这些"前面"的技巧在数 A 里**不直接送分**,而是**藏在综合题的某一步**——
> 比如算一个矩阵的极限,关键一步是 Jordan 形 + Sylvester 秩不等式。
> 见 2.12 详细展开。

### 2.3 特征多项式及相关概念矩阵

#### 特征多项式

$$
\chi_A(\lambda) \;=\; \det(\lambda I - A)
\;=\; \lambda^n - (\mathrm{tr}\,A)\lambda^{n-1}+\cdots+(-1)^n\det A
$$

要点:

```
χ_A(λ) 是 n 元多项式,首一(最高次系数=1)
  ↳ 根 = A 的特征值(在 ℂ 上一定有 n 个,计重数)
  ↳ 系数:λⁿ⁻¹ 系数 = −tr(A);常数项 = (−1)ⁿ det A
  ↳ 重要性质:相似矩阵同特征多项式  (但反过来不成立)
```

> **常考陷阱**:"$\chi_A=\chi_B$"推不出"$A\sim B$"(相似)。
> 反例:$A=\begin{pmatrix}0&1\\0&0\end{pmatrix}$,$B=0$,
> 两者 $\chi=\lambda^2$ 相同,但 $A$ 不可对角化,$B$ 已是 0,不可能相似。

#### 相关的"矩阵函数" $p(A)$

对任意多项式 $p(t)=c_kt^k+\cdots+c_0$,可定义**矩阵代入**:

$$
p(A) \;=\; c_k A^k + c_{k-1}A^{k-1}+\cdots + c_1 A + c_0 I
$$

数 A 里反复出现的有:

| 表达式 | 名称 | 含义/用途 |
|---|---|---|
| $A^T$ | 转置 | 行列互换 |
| $\overline A$ | 共轭 | 每个元素取共轭复数 |
| $A^* = \overline{A}^{\,T}$ | 共轭转置(伴随) | 复内积的核心 |
| $A^{-1}$ | 逆 | $AA^{-1}=I$ |
| $A^k$ | 幂 | 重复相乘 |
| $p(A)$ | 多项式代入 | Cayley–Hamilton: $\chi_A(A)=0$ |
| $A^\dagger$ | Moore–Penrose 伪逆 | 数 A 偶见 |

> **Cayley–Hamilton 定理**:$\chi_A(A)=0$。
> 用处极多:例 $A^{-1}=-\dfrac1{\det A}(A^{n-1}-(\mathrm{tr}A)A^{n-2}+\cdots)$,
> 即**任何可逆矩阵的逆都能写成 $A$ 的多项式**。这是数 A 证明"存在多项式表达"的钥匙。

#### 矩阵指数 $e^A$ 与矩阵函数

$$
e^A \;=\; \sum_{k=0}^{\infty} \frac{A^k}{k!}
$$

> 这个幂级数**对所有方阵都收敛**(谱半径有界即可),所以 $e^A$ 永远有意义。
> 但 $\arctan A$、$\log A$、$\sin A$ 这种就要看谱,见 2.10。

### 2.4 几何重数 vs 代数重数

这是高代里**最常考、最易混**的一对术语。它们都描述"一个特征值 $\lambda_0$ 的重数",
但**衡量对象不同**。

#### 两个定义

记 $\lambda_0$ 是 $A$ 的特征值。

```
代数重数  =  λ₀ 作为 χ_A(λ)=0 根的重数
           Algebraic multiplicity,  am(λ₀)

几何重数  =  特征子空间维数  =  dim ker(λ₀ I − A)
           Geometric multiplicity,  gm(λ₀)
```

#### 一个公式 + 一个不等式 + 一句话

$$
\boxed{\;1\le \mathrm{gm}(\lambda_0)\le \mathrm{am}(\lambda_0)\;}
$$

> 一句话:**几何重数永远不会超过代数重数**;当且仅当对每个特征值都取等,
> 矩阵**可对角化**。

#### 为什么叫"几何"vs"代数"

```
代数重数  描述  "λ₀ 在特征多项式里出现了几次"   ← 多项式(代数)层面
几何重数  描述  "λ₀ 周围有几个独立特征向量"     ← 空间(几何)层面
```

#### 最小反例(必背)

$$
A=\begin{pmatrix}2&1\\0&2\end{pmatrix}
$$

- $\chi_A(\lambda)=(\lambda-2)^2$ → $\lambda=2$ 代数重数 $=2$
- 解 $(2I-A)x=0$:$\begin{pmatrix}0&-1\\0&0\end{pmatrix}x=0$ → $x=\begin{pmatrix}1\\0\end{pmatrix}t$
  → 特征子空间维数 $=1$ → **几何重数 $=1$**
- 于是 $A$ **不可对角化**(几何重数 < 代数重数);它的标准形是 Jordan 块 $\begin{pmatrix}2&1\\0&2\end{pmatrix}$。

#### 可对角化判据(一组等价条件)

$A\in M_n(\mathbb{C})$ 可对角化 $\iff$ 下面**任一**成立:

```
① 存在 n 个线性无关特征向量
② 对每个特征值,am(λ) = gm(λ)
③ 最小多项式无重根
④ A 的 Jordan 标准形全是一阶块  (即 Jordan 形就是对角阵)
⑤ ℂⁿ 可分解为 A 的特征子空间之直和
```

> 这五条**等价**。"可对角化"在数 A 里出题,本质就是让你在这五条里挑一条证。

### 2.5 特殊矩阵全家桶(酉矩阵、Hermite、正交、正规…)

你问"矩阵有很多特殊矩阵是吗,分别有哪些"——是的,非常多,且**它们是一张关系网**。
先列总表,再画关系图。

#### 总表

| 名称 | 定义(复/实) | 关键性质 | 出现场景 |
|---|---|---|---|
| 单位阵 $I$ | 对角 1 余 0 | $IA=A$ | 基准 |
| 零阵 $O$ | 全 0 | $OA=O$ | 基准 |
| 对角阵 | 非对角 0 | 乘法按对角 | 标准形 |
| 上/下三角 | 一侧非对角 0 | 行列式=对角积 | 行列式 |
| 对称($\mathbb{R}$) | $A^T=A$ | 实特征值、可正交对角化 | 二次型 |
| Hermite($\mathbb{C}$) | $A^*=A$ | 实特征值、可酉对角化 | 复二次型 |
| 反对称 | $A^T=-A$ | 特征值纯虚/0 | 微分几何 |
| 反 Hermite | $A^*=-A$ | 特征值纯虚 | 量子味 |
| **正交**($\mathbb{R}$) | $A^TA=I$ | 保内积、$\det=\pm1$、特征值模 1 | 旋转/反射 |
| **酉**($\mathbb{C}$) | $A^*A=I$ | 复版正交、保复内积、特征值模 1 | 复对角化 |
| **正规** | $AA^*=A^*A$ | 可酉对角化(关键!) | 大一统判据 |
| **(实)正定** | $x^TAx>0,\ \forall x\ne0$ | 全部特征值 $>0$ | 二次型 |
| 幂等($A^2=A$) | 投影矩阵 | 特征值 $\in\{0,1\}$ | 投影分解 |
| **幂零**($A^k=O$) | $A$ 的某次幂为零 | 特征值全 0 | Jordan、数 A 常考 |
| 可逆 / 非奇异 | $\det A\ne0$ | 存在 $A^{-1}$ | 基础 |
| 伴随(adjugate) | 元素是余子式转置 | $A\,\mathrm{adj}(A)=\det A\cdot I$ | 求逆 |

#### 关系网(谁是谁的特例)

```
                                  一般方阵
                                      │
                          ┌───────────┴────────────┐
                       实矩阵                    复矩阵
                          │                        │
                    ┌─────┴─────┐            ┌─────┴─────┐
                  对称        正交          Hermite      酉
                 Aᵀ=A        AᵀA=I         A*=A        A*A=I
                    │           │            │           │
                    └───┬───────┘            └────┬──────┘
                        └─── 都属于 ──→   正规  AA*=A*A
                                              │
                                     (谱定理) 可酉对角化
                                              │
                                     特征:实数、模1、正定 ⟺ 全正特征值
```

#### Schur 定理(常考的"标准形"地基)

> 任意 $A\in M_n(\mathbb{C})$ 都**酉相似**于一个上三角阵:
> $A=U T U^*$,$U$ 酉,$T$ 上三角,$T$ 对角元 $=$$A$ 的特征值。

推论链(数 A 反复用):

```
   A 是正规矩阵 (AA*=A*A)
        ⇔  A 可酉对角化   (谱定理)
        ⇔  存在酉 U 使 U*A U = 对角阵
   特例:A Hermite → 对角元全实
        A 是酉矩阵 → 对角元全 |λ|=1
        A 正定(Hermite) → 对角元全 >0
```

> 一次记住:**正规 ⟺ 可酉对角化**。这一条把"对称可正交对角化"
> 这条你大学学过的东西推广到复数域、推广到所有"好矩阵"。

### 2.6 正定 / 负定二次型怎么定义、能推什么

#### 二次型的标准表述

给定**实对称**矩阵 $A\in M_n(\mathbb{R})$($A^T=A$),定义二次型:

$$
Q(x) \;=\; x^T A x \;=\; \sum_{i,j} a_{ij} x_i x_j
$$

(若 $A$ 是复 Hermite,$Q(x)=x^*Ax$,结果取实值。)

#### 正定 / 负定的定义

```
正定    ∀ x ≠ 0,  xᵀ A x > 0          记 A > 0
半正定  ∀ x,       xᵀ A x ≥ 0          记 A ≥ 0
负定    ∀ x ≠ 0,  xᵀ A x < 0          记 A < 0
半负定  ∀ x,       xᵀ A x ≤ 0          记 A ≤ 0
不定    既不是半正定也不是半负定  (∃ x,y 使 Q(x)>0, Q(y)<0)
```

> **"$x\ne0$" 这条不能漏**——$x=0$ 时 $Q(0)=0$ 恒成立,不构成"正"。
> **"$A$ 必须对称"** 也要注意:非对称矩阵的 $x^TAx$ 用对称部分 $\frac{A+A^T}{2}$ 决定,
> 所以讨论正定**默认先对称化**。

#### 等价判据(背下来,数 A 高频)

$A$ 实对称,下列**等价**:

```
① A 正定
② 所有特征值 > 0
③ 所有顺序主子式 > 0  ← Sylvester 准则,最爱考
④ ∃ 可逆 P 使 A = Pᵀ P  (合同于 I)
⑤ ∃ 唯一 Cholesky: A = L Lᵀ, L 下三角,  对角 >0
⑥ Q(x)=xᵀAx 通过合同变换化为  y₁²+…+yₙ² (正惯性指数 = n)
```

负定把上面所有 ">" 换成 "<",但**③要小心**:$A$ 负定 $\iff$ 顺序主子式
**符号交替** $(-1)^k\Delta_k>0$($\Delta_k$ 是 $k$ 阶顺序主子式)。

#### 推出来的事(能推什么)

一旦断定 $A$ 正定,你拿到一连串结论:

```
A 正定 ⇒
   ├─ det A > 0,  tr A > 0,  对角元 aᵢᵢ > 0
   ├─ A 可逆, A⁻¹ 也正定
   ├─ A^k 正定 (k 正整数)
   ├─ A 的任何主子阵都正定
   ├─ A 合同于 I,  正惯性指数 = n,  负惯性指数 = 0
   ├─ 存在唯一实对称 B 使 B² = A  (A 的"平方根")
   └─ Cauchy–Schwarz 推广:  (xᵀAy)² ≤ (xᵀAx)(yᵀAy)
```

#### 经典应用:用正定性证不等式

数 A 标准套路:把要证的不等式配成 $x^TAx\ge0$ 或 $\le0$,再用判据:

```
例:证 (Σ aᵢbᵢ)² ≤ (Σ aᵢ²)(Σ bᵢ²)    (Cauchy–Schwarz)

   考虑  Q(x,y) = (Σ aᵢ²)(Σ xᵢ bᵢ + yᵢ aᵢ)² − (Σ aᵢ(xᵢbᵢ+yᵢaᵢ))²   ← 凑形
   或更直接:  矩阵  A = [ Σa²    Σab ; Σab   Σb² ]  半正定
              因为其二阶顺序主子式 = (Σa²)(Σb²) − (Σab)² ≥ 0
              这正是要证。
```

> **二次型+正定**几乎是数 A 高代每届必出的考点。它把"代数(矩阵)"和
> "几何(曲面分类)"和"分析(不等式)"三件东西拧成一股绳,
> 是综合题最爱的接口。

---

### 2.7 $\ker$ 是什么(核)

#### 定义

线性映射 $T:V\to W$(或矩阵 $A:n\times m$ 看作 $\mathbb{F}^m\to\mathbb{F}^n$ 的映射)的**核**:

$$
\ker T \;=\; \{\,v\in V\mid T(v)=0\,\}
$$

对矩阵而言:

$$
\ker A \;=\; \{\,x\mid Ax=0\,\} \;=\; \text{方程 }Ax=0\text{ 的解空间}
$$

> **$\ker A$ 就是"$Ax=0$ 的解空间"换了个名字**。它的维数记 $\mathrm{null}\,A$ 或 $u(A)$。

#### 三个核心事实

```
① ker T 是 V 的子空间           (子空间判定直接验证)
② dim V = dim ker T + dim Im T  ← "秩-零度定理"(rank–nullity),高频
③ T 单射 ⟺ ker T = {0}          (核平凡 = 既单又满的"单"那一半)
```

#### ASCII 图

```
        V (定义域)                   W (陪域)
   ┌──────────────────┐          ┌──────────────────┐
   │                  │          │                  │
   │   ker T  ◄──┐    │   T      │      Im T ◄──┐    │
   │   {v:T v=0}  │    │ ─────→  │      像集     │    │
   │              │    │          │              │    │
   │   其余 v ────┼────┼──→ T(v) ─┼──────────────┘    │
   │              │    │          │                  │
   └──────────────────┘          └──────────────────┘
        dim V = nullity(T)  +  rank(T)
                ↑                   ↑
              ker 维数             Im 维数
```

#### 出现的典型考法

```
"求所有 x 使 Ax=0"           → 求 ker A 的一组基
"判断 T 是否可逆"             → 看 ker T 是否 = {0}
"求不变子空间"                → ker(T−λI) 正是特征子空间!
"对角化失败的原因"            → 几何重数 = dim ker(λI−A) < 代数重数
```

> 看出来没:**特征值、特征向量、可对角化,本质都是 $\ker$ 的故事**。
> "$\lambda$ 是特征值" $\iff \ker(\lambda I-A)\ne\{0\}$。
> 这就是为什么高代后半段 $\ker$ 反复出现——它是抽象统一语言。

### 2.8 $\oplus$(圆圈十字)——直和

你问的"圆圈中间一个十字"就是 $\oplus$,读"**直和**"(direct sum)。

#### 直观含义

$V = U \oplus W$ 表示:**$V$ 里每个向量都能**唯一**拆成 $U$ 的一个 + $W$ 的一个**:

$$
\forall v\in V,\ \exists!\ u\in U,\ w\in W,\ \text{使 } v=u+w
$$

"唯一"等价于 $U\cap W=\{0\}$。

```
   V 用直和把 U 和 W "拼起来":

       V
   ┌───────┐
   │ U  │  │     任取 v ∈ V
   │    │W │     v = u + w  (u∈U, w∈W)
   │    │  │     而且 u, w 由 v 唯一决定
   └───────┘     等价:U ∩ W = {0}
                等价:dim V = dim U + dim W  + 交为 0
```

#### 几个最常见的出现场景

```
① 欧氏空间向量分解  ℝ³ = (xy 平面) ⊕ (z 轴)
② 特征子空间分解    A 可对角化 ⟺ ℂⁿ = ⊕ᵢ V_λᵢ  (各特征子空间直和)
③ Jordan 分解       ℂⁿ = ⊕ 广义特征子空间 (按特征值)
④ 投影构造          P²=P ⟺ V = ker P ⊕ Im P
⑤ 不变子空间分解    T 可对角化 ⟺ V 能分解为 T 的一维不变子空间直和
```

#### 为什么要"直和"而不是普通"和"

普通的"和"$U+W$ 只要 $u+w$ 形式能凑出 $V$ 即可,**不要求唯一**。
但"不唯一"意味着 $U\cap W$ 有非零元素,信息有冗余。直和 = **无冗余**分解,
所以维数公式才漂亮:$\dim(U\oplus W)=\dim U+\dim W$。这是抽象统一的基础。

> ★ 一个数 A 标准句式: "$\mathbb{C}^n = V_{\lambda_1}\oplus V_{\lambda_2}\oplus\cdots\oplus V_{\lambda_k}$"
> 一出现你就知道:**$A$ 可对角化、不同特征值的特征向量彼此独立**。

#### 旁边一个形似但不同的符号:$\otimes$(圆圈叉,张量积)

```
⊕  直和       维数相加     dim(U⊕W)=dim U+dim W     ★ 数 A 常见
⊗  张量积     维数相乘     dim(U⊗W)=dim U × dim W   △ 数 A 基本不考
```

字形:⊕ 中间是**加号**(对应"维数相加"),⊗ 中间是**乘号**(对应"维数相乘")。
你看到的"圆圈十字"几乎一定就是直和 $\oplus$。

### 2.9 线性算子的考点与符号

你问"线性算子方面好像有很多考点,这些哪里来的、有没有新运算符号"。
答案是:**有的,而且这是一整套抽象符号体系**,你大学线代基本没碰。

> **"线性算子"** 就是"线性空间到自身的线性映射" $T:V\to V$。
> 矩阵只是它在**选定基**下的**表示**;脱离基,$T$ 本身是个抽象对象。
> 数 A 高代的"硬"题几乎都在这一层。

#### 必须认识的符号表

| 符号 | 名称 | 含义 |
|---|---|---|
| $T$ / $\mathscr{A}$ / $\mathcal{A}$ | 线性算子(变换) | $V\to V$ 的线性映射 |
| $\mathscr{A}(V)$ / $\mathrm{Im}\,T$ | 像 / 值域 | $\{Tv: v\in V\}$ |
| $\ker T$ / $\ker\mathscr{A}$ | 核 | $\{v:Tv=0\}$ |
| $T(V_0)$ | 子空间像 | 子空间 $V_0$ 在 $T$ 下的像 |
| $T^{-1}(W_0)$ | 原像 | $W_0$ 在 $T$ 下的原像子空间 |
| $[T]_{\mathfrak{B}}$ / $A_{\mathfrak{B}}$ | 表示矩阵 | $T$ 在基 $\mathfrak{B}$ 下的矩阵 |
| $T_1T_2$ / $T_1\circ T_2$ | 复合 | 先 $T_2$ 后 $T_1$ |
| $T^*$ | 伴随 | 内积空间里 $\langle Tx,y\rangle=\langle x,T^*y\rangle$ |
| $T^{-1}$ | 逆变换 | $T$ 可逆时 |
| $T^k$ | 幂 | $T\circ\cdots\circ T$ |
| $p(T)$ | 算子多项式 | $c_kT^k+\cdots+c_0I$ |
| $V_\lambda$ | 特征子空间 | $\ker(T-\lambda I)$ |
| $W$ **$T$-不变** | 不变子空间 | $T(W)\subseteq W$ |
| $V/W$ | 商空间 | 模掉 $W$ 的等价类 |
| $\mathscr{A}|_W$ | 限制 | $T$ 限制到子空间 $W$ 上 |

#### "考 点"从哪里来

数 A 高代大题的考点分布,几乎就是上面这些概念之间的关系:

```
核心考法                                  用到的符号/概念
─────────────────────────────────────────────────────────
判 T 可对角化 / 求 Jordan 形              特征子空间、不变子空间、最小多项式
求 T 的不变子空间                          T-不变、ker p(T)
T 在某基下的表示矩阵                       [T]_𝔅、相似、 changement de base
T 的多项式 / 最小多项式                    p(T)、m_T(λ)、Cayley-Hamilton
内积空间上的 T (正规/Hermite/酉)           T*、谱定理、酉对角化
T 的分解 (谱分解、Jordan 分解)             直和 ⊕、广义特征子空间
对偶空间 / 对偶基                          T^t (对偶变换)、V*            △偶考
商空间上的诱导变换                          V/W、T̄                        △偶考
```

#### 表示矩阵的"换基公式"(几乎每届都用到)

设 $\mathfrak{B}_1,\mathfrak{B}_2$ 是两组基,$P$ 是 $\mathfrak{B}_1\to\mathfrak{B}_2$ 的过渡矩阵
($\mathfrak{B}_2 = \mathfrak{B}_1 P$),则 $T$ 在两组基下的表示矩阵:

$$
[T]_{\mathfrak{B}_2} \;=\; P^{-1}\,[T]_{\mathfrak{B}_1}\,P
$$

> 这就是"相似"的来源:**同一算子在不同基下的矩阵彼此相似**。
> 反过来,"求 Jordan 标准形"="找一个基,使 $T$ 的表示矩阵最简"。

### 2.10 "$\arctan A$ 有意义"——矩阵函数黑话

#### 黑话破译

如题面出现"求使 $\arctan A$ 有意义 / $\log A$ 有意义 / $\sqrt A$ 有意义"的条件,
**"有意义"= 该矩阵函数能被良好定义**。两条主要定义路径:

```
路径①  幂级数定义
   对数函数 f(z)=Σ cₖ zᵏ (收敛半径 R),
   定义 f(A)=Σ cₖ Aᵏ,
   要求 A 的"谱半径" ρ(A) < R,级数才收敛.

路径②  Jordan 形定义(更一般)
   把 A 写成 A = P J P⁻¹, J 是 Jordan,
   在每个 Jordan 块 Jₖ(λ) 上定义 f(Jₖ),
   要求 f 在 λ 处有充分多阶导数.
```

#### "有意义"翻译成可执行条件

| 函数 | "有意义"的可执行条件 | 原因 |
|---|---|---|
| $e^A$ | **永远有意义** | 幂级数全网收敛 |
| $\sin A,\cos A$ | **永远有意义** | 同上 |
| $\arctan A$ | $A$ 的特征值不在 $\pm i$ 上 | $\arctan$ 在 $\pm i$ 处有奇点 |
| $\log A$ | $A$ 没有特征值 $\le 0$(或更精:在选定支上无奇点) | $\log$ 在 $\le0$ 实轴有支点 |
| $\sqrt{A}$ | $A$ 没有"沿非正实轴"的特征值(或:$A$ 可对角化+无负实特征值) | 平方根支点 |
| 任意 $f(A)$ | $f$ 在 $A$ 谱上有充分阶导数 | 函数矩阵一般定义 |

> 实际数 A 出题套路:**先让你求"有意义"的条件(即分析 $A$ 的谱满足什么)**,
> 再让你算 $f(A)$ 的具体形式或证性质。两部分常连成一道大题。

#### 一个最小例子

> 设 $A=\begin{pmatrix}0&-1\\1&0\end{pmatrix}$,$A$ 的特征值是 $\pm i$。
> 所以 $\arctan A$ **没有意义**(奇点正好在谱上);
> 而 $e^A$ 一定有意义,可算出 $e^A=\begin{pmatrix}\cos1&-\sin1\\\sin1&\cos1\end{pmatrix}$。

### 2.11 矩阵与极限结合的考法

你问"矩阵还能和极限结合吗,真的跟矩阵性质结合的那种" —— **能,而且相当精彩**。
这是高代和分析的接口处,数 A 偏爱。

#### 矩阵序列收敛

$A_k\to A$ 定义:每个元素收敛,即 $(A_k)_{ij}\to A_{ij}$。
等价刻画(在有限维下都等价):

$$
A_k\to A \iff \|A_k-A\|\to 0\quad(\text{任意矩阵范数})
$$

#### 矩阵级数 + 谱半径

最经典的"极限+矩阵性质"考法是 **Neumann 级数**:

$$
(I-A)^{-1} \;=\; \sum_{k=0}^{\infty} A^k \quad\Longleftrightarrow\quad \rho(A)<1
$$

其中 $\rho(A)=\max|\lambda_i(A)|$ 是**谱半径**(特征值模的最大值)。

```
连接矩阵"极限/级数" ↔ "矩阵特征值性质"的桥梁:
   ρ(A) < 1  ⟺  Aᵏ → 0  ⟺  (I−A) 可逆且 ∑ Aᵏ 收敛
   ρ(A) ≥ 1  ⟹  Aᵏ 不趋于 0
```

> 这把"计算层面的级数收敛"和"代数层面的谱"焊在一起。
> 题面给一个含参数矩阵 $A(t)$,问"何时 $\sum A(t)^k$ 收敛",
> 你立刻翻译成"何时 $\rho(A(t))<1$",再翻译成"$A(t)$ 特征值模都 $<1$"——
> 一道矩阵极限题就变成了特征值题。

#### 其他常见结合点

```
① 矩阵幂极限   lim Aᵏ 是否存在 / 等于 0?
                ↳ 由谱半径 + Jordan 块结构判定
② 矩阵指数微分  d/dt e^{tA} = A e^{tA} = e^{tA} A   ← ODE 接口
③ 离散动力系统 x_{k+1} = A x_k,问 x_k 收敛性
                ↳ Aᵏ → 0 ⟺ ρ(A)<1
④ 矩阵对角化 + 数列  递推数列 a_n 用矩阵 A 的幂表示,极限看谱
⑤ Cayley-Hamilton 算极限  Aᵏ 用低次幂线性表示,化幂级数为有限项
```

> 这是数 A 里"非数学专业最容易抓瞎但题型套路最固定"的部分。
> 看到"矩阵 + 极限/级数",反射弧:**谱半径 $\rho(A)$ + Jordan 结构**。

### 2.12 高代前面的竞赛技巧(行列式 / 方程组 / 分块)

你说"前面是不是也有竞赛技巧" —— 有,而且**这部分是你大学线代能直接吃下的红利**。
数 A 把它考得偏技巧,但不抽象,练熟就能拿分。

#### (a) 行列式技巧

```
① Vandermonde 公式
   det 1…xᵢ^(n-1)  = ∏_{i<j}(xⱼ − xᵢ)
   ↳ 看到行/列成等比就条件反射 V 论

② 拆分 / 加边法
   把某行拆成两部分,行列式拆成两个

③ 递推降阶
   D_n 用展开得到 D_n = a D_{n−1} − b D_{n−2},解特征根

④ 用 det(AB)=det A · det B
   把难算的 det XY 拆成两个易算的

⑤ 分块三角
   det [A B ; 0 D] = det A · det D
   det [A 0 ; C D] = det A · det D

⑥ Schur 补
   A 可逆时  det [A B ; C D] = det A · det(D − C A⁻¹ B)
```

#### (b) 矩阵 / 秩技巧

```
① 秩不等式(背)
   r(A+B) ≤ r(A)+r(B)
   r(AB) ≥ r(A)+r(B) − n        (Sylvester)
   r(AB) ≤ min{r(A),r(B)}
   r(A)+r(B) − n ≤ r[A B] ≤ r(A)+r(B) … 等变体

② Frobenius 不等式  r(ABC) ≥ r(AB)+r(BC) − r(B)

③ 乘积为零 ⟹  r(A)+r(B) ≤ n  (若 AB=0)

④ 分块技巧
   [I 0 ; −CA⁻¹ I] [A B ; C D] = [A B ; 0 D−CA⁻¹B]
   ↳ 既算行列式又算逆

⑤ 行/列满秩刻画
   r(A)=n ⟺ A 左可逆(列满秩)
   r(A)=m ⟺ A 右可逆(行满秩)
```

#### (c) 方程组 / 解空间

```
① 解空间维数定理   dim{kex A} = n − r(A)
② AX=B 的解        = 一个特解 + ker A
③ AB=0 ⟹ 列(B) ⊂ ker A   →  r(A)+r(B) ≤ n
④ 把"解集"看作子空间,直接进入线性空间语言
```

#### (d) 多项式技巧(高代独立一块)

```
① 带余除法     f = q g + r,  deg r < deg g
② 互素         (f,g)=1 ⟺ ∃ u,v 使 uf+vg=1
③ Eisenstein   在 ℚ 上判不可约
④ 有理根定理   ℚ 上多项式有理根 p/q 满足 p|a₀, q|aₙ
⑤ Vieta        根与系数关系
⑥ 重根判别     f 与 f' 互素 ⟺ f 无重根
⑦ 实/复数域    ℂ 完全分解一次,ℝ 分解到一次或二次
```

> **小结**:2.12 这一节的技巧**难度低于 Part 2 前面那些抽象层**,
> 但**出现频率极高**——综合题里多半会有一步用到它们。
> 你的训练策略:**Part 2.1–2.11 是补"概念空白",2.12 是补"技巧熟练度"**。
> 前者优先,后者反复刷真题积累。

---

## Part 3 解析几何篇

这一部分回答你三个具体问题,外加一个整体疑惑:
- 3.1 "数 A 还有建系?和高中解析几何有什么不同?"
- 3.2 "直线旋转得到的曲面来自哪个曲面的性质?其他曲面也有类似性质吗?都是重点考点吗?"
- 3.3 "脐点是什么?有哪些考法和求法?类似高考哪类题型?"
- 3.4 "挠率是什么?英文原文?怎么定义计算?和曲率什么关系?"

### 3.1 数 A 解几 vs 高中解几(整体定位 & "建系")

#### 维度跃迁

| | 高中解几 | 数 A 解几 |
|---|---|---|
| 主要空间 | **二维**平面 | **三维**空间(局部二维曲面) |
| 核心对象 | 圆锥曲线(椭圆/双曲/抛物) | 二次曲面 + 空间曲线 + 坐标变换 |
| 工具 | 直角坐标、距离公式 | 向量法、叉积、混合积、**齐次坐标**、**仿射坐标** |
| 重点 | 求轨迹、切线、离心率 | 标准形分类、切平面/法线、截面、**不变量**、变换群 |
| "建系"含义 | 选个 xy 原点 | 选基 + 选仿射坐标系(可不正交) + 旋转平移把曲面**主轴化** |

#### "建系"指的是什么

数 A 解几里的"建系"比高中多两层:

```
高中建系:        选个原点,摆正 x、y 轴 →  写方程
                                    │
                                    ▼
数 A 建系:     选基 {e₁,e₂,e₃}  ──→  仿射坐标系(可不正交,可不通为单位长)
                                    │
                正交化/单位化  ──→  直角(欧氏)坐标系
                                    │
                对曲面方程做旋转平移 ──→  主轴化(化成标准形),判类型
```

> 高中解几"建系"几乎一步完成;数 A 解几"建系"是**一套操作**:
> 选坐标系 → 写方程 → 通过坐标变换(正交/仿射)把曲面方程化到标准形
> → 用标准形分类讨论。这一整套就是"二次型 + 合同变换"在几何上的应用。
> 所以解几和高代是**同一个二次型问题**的两个面孔——代数面孔是"合同标准形",
> 几何面孔是"曲面主轴化"。Part 2.6 和 3.2 在这里接上。

#### 考纲要点回顾(来自考试范围文档)

```
解析几何(15%):
  ✓ 向量与坐标     内积、叉积、距离、投影、方向关系
  ✓ 平面与直线     一般式/参数式,位置关系,夹角和距离
  ✓ 二次曲线       标准方程、切线/法线、焦点与离心率、二次型化简
  ✓ 空间曲面与曲线  球面、柱面、锥面、二次曲面及截面
  ✓ 仿射与正交变换  坐标变换、旋转和平移、二次曲线/曲面的分类
```

> 注意:**官方没有单列"曲率/挠率/脐点"**。它们属于"空间曲面与曲线"的延伸,
> 是**边界内容**——有的届出现(尤其压轴小问),有的届不出现。
> 你看到的"很喜欢考这种"印象,是对的,但要分清:
> **切平面/法线/截面/直纹面**这类是稳定高频;**脐点/曲率/挠率**是偶发较难。

### 3.2 直线旋转得到的曲面 & 直纹面家族

#### 直接回答

**一条直线绕与之异面的轴旋转,得到的是单叶双曲面**。这与单叶双曲面的一个**惊人
性质**直接挂钩:它是**直纹面**(ruled surface)——可以**完全由一族直线铺成**。

```
   单叶双曲面   x²/a² + y²/b² − z²/c² = 1   ← 一个曲面
        │
        │  不可思议的性质:
        ▼
   它身上恰好有两族直线 "编织" 出整个曲面
   (你完全可以拿一根根直钢丝焊出它)
        │
        ▼
   旋转视角:取其中一根直母线,绕 z 轴转一圈 → 得到整个单叶双曲面
```

#### 一般地,"曲线旋转得到的曲面"通用规律

平面曲线 $\Gamma$ 绕轴旋转得到的曲面叫**旋转曲面**。母线决定类型:

| 母曲线(在 $xz$ 平面) | 绕 $z$ 轴旋转 | 得到的曲面 |
|---|---|---|
| 圆 $(x-a)^2+z^2=R^2$ | 绕 $z$ | 圆环面(torus) |
| 圆 $x^2+z^2=R^2$ | 绕 $z$ | 球面 |
| 椭圆 $x^2/a^2+z^2/c^2=1$ | 绕 $z$ | 旋转椭球面 |
| 抛物线 $x^2=2pz$ | 绕 $z$ | 旋转抛物面 |
| 双曲线 $x^2/a^2-z^2/c^2=1$ | 绕 $z$(实轴) | **单叶**旋转双曲面 |
| 双曲线 $x^2/a^2-z^2/c^2=1$ | 绕 $x$(虚轴) | **双叶**旋转双曲面 |
| 直线 $x=kz$ | 绕 $z$ | 圆锥面($k\ne0$)/ 圆柱($k=0$) |
| 直线(与轴**异面**) | 绕轴 | **单叶双曲面**(一般形,非旋转形) |

> **法则是**:"母线方程里的 $x$ 代换成 $\pm\sqrt{x^2+y^2}$"。
> 例:$x^2/a^2-z^2/c^2=1$(母线) → 绕 $z$:
> $(x^2+y^2)/a^2-z^2/c^2=1$,正是单叶双曲面。

#### "其他曲面也有类似难以想到的数学性质吗?"

有。这些"反直觉"性质几乎全部集中在**直纹面家族**和**二次曲面特殊情况**:

```
曲面               低调但常考的性质
─────────────────────────────────────────────────────────
单叶双曲面         直纹面(两族直母线)、负高斯曲率、包含直线
双曲抛物面(马鞍)  直纹面(两族直母线)、负高斯曲率、z=x²/a²−y²/b²
锥面               退化直纹面、过顶点
柱面               平移型直纹面
椭球面             有4个脐点(若三轴不等长)、所有截面是椭圆
球面               所有点都是脐点、处处正曲率
环面               内侧负高斯曲率、外侧正高斯曲率、有两条抛物线
```

#### "都是重点考点吗?我看 A 类好像很喜欢考这种"

是的,**但分两类**:

```
稳定高频（几乎每届）：
  ✦ 切平面 / 法线 / 切锥面  ← 第16届题1就是这个味
  ✦ 截面曲线 / 交线
  ✦ 二次曲面分类 / 标准形
  ✦ 直线与曲面的相切、位置关系
  ✦ 通过直纹面构造曲面

偶发较难（若干届出现）：
  ✦ 直纹面的直母线族
  ✦ 脐点（见 3.3）
  ✦ 曲率 / 挠率（见 3.4，更像微分几何味）
```

> 经典例子(第 16 届题 1):曲面 $S: x^2+y^2-z^2=-2$(双叶双曲面),
> 过 $M_0(1,1,-1)$ 的切线全体构成**切锥面** $\Sigma$,
> 再求 $S^+\cap\Sigma$ 所在平面 $\pi$。
> 看到了?**曲面的切线 → 锥面 → 截面 → 平面**这一串,
> 正是"切平面/切锥面/截面"高频家族的代表。

### 3.3 脐点(umbilic point)

#### 是什么

**脐点** = 曲面上**两个主曲率相等**的点,也就是该点处**沿任何方向法曲率都相同**。
形象说:在这个点附近,曲面"弯得像球面"——所有方向一样弯。

```
   一般点:                脐点:
   方向不同 → 法曲率不同    任意方向 → 法曲率相同  (像个小球面贴上去)
                                                        ↷
            ↶                                          ( 球面感 )
                                                       ↶
            ↶                                        ( 全部一样 )
   (两个主方向,两个主曲率 κ₁≠κ₂)                     κ₁=κ₂=κ
```

#### 例子(建立直觉)

```
球面        每一点都是脐点(处处 κ₁=κ₂,完全各向同性)
平面        每一点都是脐点(κ₁=κ₂=0,退化脐点)
椭球面      (三轴不等长)恰有 4 个脐点  ← 经典结论
抛物面      一般有 1 个脐点(顶点附近)
单叶双曲面  无脐点(负高斯曲率,主曲率异号)
```

> **球面处处脐点**这个事实,可作快速判定题的答案。

#### 求法(标准化流程)

设曲面 $S: F(x,y,z)=0$, $P\in S$。

```
① 写出第一基本形式  E,F,G    (度量,来自 r_u, r_v)
② 写出第二基本形式  L,M,N    (曲率,来自 r_{uu}·n 等)
③ 主曲率是方程   |L−κE   M−κF   |
                   |M−κF   N−κG  | = 0   (即 det(II−κI)=0)  的两根 κ₁,κ₂
④ 脐点条件   ⟺  两个根相等 ⟺ 判别式=0
            ⟺ (在适当坐标系下)  L/E = M/F = N/G   (II 与 I 成比例)
⑤ 解这组条件给出的方程组 → 得脐点位置
```

#### 类似高考哪类题型

像**高考"圆锥曲线 + 切线 + 求特殊点"**的**三维升级版**:

```
高考题(二维):  抛物线 y²=2px,求其焦点,确认特殊点。
                思路:写方程 → 配方 → 找特殊几何条件。

数A脐点(三维): 曲面 F=0,求脐点。
                思路:写 I,II → 主曲率方程 → 等根条件 → 解方程组。
```

难度在于中间多了"第一、第二基本形式"两套量。但骨架和高考那种"由方程→求特殊点"完全一样。

#### 出题套路

```
① 直接求给定曲面的脐点                (固定流程)
② 证某曲面无脐点 / 恰有 N 个脐点       (条件不等式)
③ 证球面处处脐点 / 反之椭球仅 4 个     (理论题)
④ 与主曲率/高斯曲率结合                (κ₁κ₂=K, κ₁+κ₂=H)
```

> 高斯曲率 $K=\kappa_1\kappa_2$,平均曲率 $H=\frac{\kappa_1+\kappa_2}{2}$。
> 脐点 $\kappa_1=\kappa_2 \Rightarrow K=\kappa^2$, $H=\kappa$。
> 这两个量在脐点处关系最简单,所以常出现在题里。

### 3.4 曲率 $\kappa$ 与挠率 $\tau$(torsion)

#### 英文 & 直觉

```
curvature   κ   "bending"    描述曲线 弯曲 得多厉害
torsion     τ   "twisting"   描述曲线 扭出 密切平面 的程度
```

形象图(想象一条盘旋着上升的弹簧):

```
                ╲╱
              ╱     ╲          弯曲(curvature κ):
            ╱   ↑     ╲          曲线沿主法向 N 偏离切向 T 的速率
            ╲   │N    ╱
              ╲ │  ╱╲            扭转(torsion τ):
                T    ╲╱            密切平面本身在旋转的速率
                       B (副法向)
   平面曲线(整条躺在一张平面里):  τ ≡ 0  (没有扭转,只在自身平面内弯)
   直线:                          κ ≡ 0  (根本不弯)
   螺旋线:                        κ, τ 都 > 0,且都常数 → 最经典的"双重弯"
```

#### Frenet 标架(T, N, B)

对弧长参数 $s$ 的曲线 $\mathbf{r}(s)$:

```
T(s) = r'(s)            单位切向       (走的方向)
N(s) = T'(s)/|T'(s)|    单位主法向     (弯的方向)
B(s) = T × N            单位副法向     (扭的方向)
```

#### Frenet 公式(必背)

$$
\begin{pmatrix}T'\\N'\\B'\end{pmatrix}
=
\begin{pmatrix}0&\kappa&0\\-\kappa&0&\tau\\0&-\tau&0\end{pmatrix}
\begin{pmatrix}T\\N\\B\end{pmatrix}
$$

即:
$$
T'=\kappa N,\quad N'=-\kappa T+\tau B,\quad B'=-\tau N
$$

#### 计算公式(任意参数 $t$)

不必非要弧长参数。对一般参数曲线 $\mathbf{r}(t)$:

$$
\boxed{\;\kappa \;=\; \frac{\|\mathbf{r}'\times\mathbf{r}''\|}{\|\mathbf{r}'\|^3}\;}
$$

$$
\boxed{\;\tau \;=\; \frac{(\mathbf{r}'\times\mathbf{r}'')\cdot\mathbf{r}'''}{\|\mathbf{r}'\times\mathbf{r}''\|^2}\;}
$$

> **一句记忆**:**曲率用 1 阶、2 阶导**算(够用就弯);
> **挠率还要 3 阶导**(扭转是更高阶的几何量)。

#### $\kappa$ 与 $\tau$ 的关系

```
κ = 0              直线           τ 无意义(退化)
κ > 0, τ = 0       平面曲线
κ > 0, τ = const   螺旋线         (常曲率 + 常挠率 ⟺ 一般螺旋线)
κ,τ 都任意         一般空间曲线    (由 Frenet 公式唯一决定形状,差刚体运动)
```

> 经典对:**曲率 + 挠率**(两个标量函数)**完全决定**(在刚体运动意义下)一条空间曲线的形状。
> 这就是为什么 $\kappa,\tau$ 是曲线的"内在不变量"。

#### 数 A 中怎么考

```
① 计算题: 给 r(t),求 κ,τ           (套公式,送分但易算错)
② 证明题: 证明某曲线 τ≡0 ⟺ 是平面曲线
③ 性质题: 常曲率常挠率曲线是螺旋线
④ 结合题: 与 Frenet 标架、曲线积分结合
```

> **诚实定位**:曲率挠率这块**严格说偏微分几何**,数 A 解几官方范围没明列,
> 但因"空间曲线"项可解释进范围,部分届会作为压轴一小问出现。
> 优先级低于 3.2 的曲面题;**学有余力时补,不在第一轮补短板清单里**。

---

## Part 4 决赛扩展篇澄清 — 你看到的"天书题"真相

> ★ **这是整份手册对你最重要的提醒之一,请认真读完。**

你清单里有一批题目让你"一个字都没看懂""啊啊好抽象":
- "证明:180 阶群不是单群"
- 环论、理想
- 庞加莱上半平面
- 域扩张
- 独立随机变量与矩阵结合

**它们绝大多数根本不在初赛 A 类的考试范围里。** 你看到的应该是**决赛高年级组选做题**
(或者 B 类、或别的资料混进来的题)。先回到官方范围:

```
官方通知明确的范围:
  初赛 A 类 = 数学分析 + 高等代数 + 解析几何   (50/35/15)
  
决赛扩展:
  低年级组 = 初赛范围 + 常微分方程(约 15%)
  高年级组 = 上述 + 在【实变函数、复变函数、抽象代数、数值分析、
                      微分几何、概率论】中选做若干题
```

所以:

```
你看的题面提到的科目          在初赛 A 吗?
─────────────────────────────────────────────
群、单群、Sylow              ✗  抽象代数(决赛高年级选做)
环、理想、整环、唯一分解      ✗  抽象代数(决赛高年级选做)
域扩张、Galoi 群              ✗  抽象代数(决赛高年级选做)
庞加莱上半平面                ✗  微分几何 / 复变(决赛高年级选做)
概率分布 + 矩阵              ✗  概率论(决赛高年级选做)
实变、Lebesgue 积分           ✗  实变函数(决赛高年级选做)
微分方程定性理论              △  属决赛低年级新增(初赛 A 不考)
─────────────────────────────────────────────
多项式在 ℚ/ℝ/ℂ 上不可约      ✓  在初赛 A 高代范围!
```

> **结论**:你被"卷子怎么这么难"吓到,有相当一部分是因为看的资料
> **混入了决赛题**。先把这些识别掉,你的真实战场就清爽很多。
> 下面逐个**简短**解释这些概念是什么(满足好奇 + 帮你识别),但**不要花时间深学**。

### 4.1 "证明:180 阶群不是单群"——这是什么题

#### 题面翻译

```
"群"     group        一种代数结构:集合 + 一个运算,满足封闭/结合/有单位/有逆
"阶"     order        有限群里元素的个数  →  "180 阶" = 这群有 180 个元素
"单群"   simple group 没有非平凡正规子群  (类比"素数":不能进一步分解)
"不是单群" 你要证明 |G|=180 的群 G 一定存在非平凡正规子群
```

#### 属于哪一节、邻居是谁

```
抽象代数大纲(决赛高年级选做):
├ 第1章 群论基础
│   ├ 群、子群、陪集、Lagrange 定理
│   ├ 正规子群、商群、同态定理
│   ├ ★ Sylow 定理       ← "180 阶群不是单群"用这个
│   └ 群作用、轨道、共轭
├ 第2章 环论
│   └ 理想、整环、UFD、PID
└ 第3章 域论
    └ 域扩张、Galoi 理论
```

**邻居知识点**:
- **Lagrange 定理**($|H|$ 整除 $|G|$)
- **Sylow 定理**($|G|=p^a m$, $p\nmid m$ 时存在 $p^a$ 阶子群,且 Sylow $p$-子群个数 $\equiv1\pmod p$ 且整除 $m$)
- 这题思路大致是:$180=2^2\cdot 3^2\cdot 5$,用 Sylow 第三定理说 Sylow 子群个数受限,推出存在唯一(从而正规)的 Sylow 子群,故非单。

#### 你需要做什么

**啥也不用做**。这题**不在初赛 A 范围**。识别到"群、阶、单群"等词,
判定为决赛高年级抽代选做题,**直接跳过**。

### 4.2 "证明不可约" 属于什么题?(要分两种情况)

**关键**:同一个词"不可约",**初赛 A 考一种,决赛考另一种**。

```
情况① 多项式不可约   ←  ✓ 初赛 A 高代范围(必学)
  例:证明 x⁴+1 在 ℚ 上不可约;判 x³−2 在 ℚ 上不可约
  方法:Eisenstein 判别、有理根定理、模 p 化、根的存在性讨论
  属于:高代"多项式"一章
  
情况② 环/代数上不可约  ←  ✗ 决赛高年级抽代
  例:ℤ[x] 中某理想是否极大;ℂ[x,y] 中多项式是否不可约
  方法:环上既约元、不可约元、UFD/PID 中的定义
  属于:抽代"环论"
```

> **判别方法**:看题面有没有 $\mathbb{Q},\mathbb{R},\mathbb{C}$ 这种**数域**限定词。
> 有,且问的是多项式 → 初赛 A 范围;只有抽象的"环 $R$"→ 决赛范围。

### 4.3 环论 & 理想(一句话扫盲,不深学)

```
环 ring      ≈  "可以做加、减、乘(但不一定有除法)的集合"
                例:ℤ 整数环, ℝ[x] 实多项式环, Mₙ(ℂ) 矩阵环
理想 ideal   ≈  "环里像'倍数'那样的子集"
                例:ℤ 中 2ℤ = {偶数} 是一个理想
                正式定义:I 是 R 的子环,且 ∀r∈R, ∀a∈I, ra∈I
```

> 直觉:**理想是环论里"子群+陪集" 的对应物**,用来构造**商环**(就像正规子群构造商群)。
> 它是抽代关键工具,但**初赛 A 不考**。

### 4.4 庞加莱上半平面(一句话扫盲)

$$
\mathbb{H} = \{z\in\mathbb{C}\mid \mathrm{Im}\,z > 0\}
$$

复平面上半部分,配上**双曲度量** $ds^2 = (dx^2+dy^2)/y^2$,就成了
**双曲几何的标准模型**之一。是微分几何 / 复变函数里反复出现的舞台。

> 这是**微分几何/复变**的内容,决赛高年级选做。**初赛 A 不考**。

### 4.5 域扩张(一句话扫盲)

```
域扩张 K/F   =  K 是 F 的扩域 (F ⊂ K,两者都是域)
                 例:ℂ/ℝ,  ℝ/ℚ,  ℚ(√2)/ℚ
[ K : F ]   =  扩张次数(作为 F-线性空间的维数)
代数扩张、超越扩张、Galoi 扩张…… 后面接 Galoi 理论
```

> 用来研究"方程有没有根式解"这种大问题。**抽代决赛范围,初赛 A 不考**。

### 4.6 概率 + 矩阵结合(一句话扫盲)

你问的"独立随机变量和矩阵结合"这种题,典型样貌:

```
设 X 是随机向量,X 的协方差矩阵 Σ = E[(X−μ)(X−μ)ᵀ],
求 Σ 的特征值性质 / 求 E[Xᵀ A X] / 用矩阵分析多元正态分布……
```

属于 **多元统计分析 + 概率论 + 矩阵论**的交叉,决赛高年级概率论选做题。
和你在大学概率论里学的"两个随机变量独立"是**同一概念的多维推广**,
但**初赛 A 不考概率**。

### 4.7 给你的"识别 + 跳过"快速判别表

看到题面出现下面这些词,**立即判定为非初赛 A 范围,跳过**:

```
✗ 群、子群、Sylow、正规子群、单群、同构定理、Galois
✗ 环、理想、整环、UFD、PID、Noether
✗ 域扩张、代数扩张、分裂域
✗ 测度、Lebesgue 积分、可测函数、σ-代数
✗ 解析延拓、留数、Cauchy 积分定理(复变函数层面)
✗ 庞加莱模型、双曲几何、Riemann 曲率张量
✗ 概率分布、期望、协方差矩阵、马氏链
✗ ODE 定性理论、Lyapunov 稳定、相图
```

> 看 7 套初赛真题,这些词**一道都不会出现**。如果你在某本资料里看到,
> 那个资料的范围比你需要的宽,你需要**主动屏蔽**它们。

---

## Part 5 收尾 — 非数学专业挑战数 A 的补短板路线图与心态

### 5.1 你已经有的(可迁移优势)

不是从零起步。你的大学课程已经覆盖了一部分,只是没到数 A 严格性:

```
✓ 极限/连续/微积分计算   → 加 ε-δ 语言、严格论证即可
✓ 一元/多元积分计算      → 加可积性判定、积分不等式、含参积分
✓ 矩阵/行列式/方程组     → 加分块、秩不等式、结构化技巧
✓ 特征值/对角化基础      → 加最小多项式、Jordan 形、正规矩阵
✓ 向量/平面/直线         → 基本够,加二次曲面分类 + 切平面/截面
✓ 高中解几               → 升维到三维 + 二次型化简视角
✓ 离散里的群/环入门      → 帮你识别(初赛 A 不考,识别即可)
✓ 概率论基础             → 帮你识别(初赛 A 不考)
```

> **可迁移的最大优势是"算得动 + 算法思维"**。数 A 卷里有一半分值是
> "带计算的证明/推导题",你的计算底子在,这部分是你**能抢的分**。

### 5.2 补短板优先级路线图(一张图说清顺序)

```
                  优先级从高到低
   ┌────────────────────────────────────────────────────────────┐
   │ 阶段 ①  数分地基(必补,占比最大)                  高优先  │
   │  ─ ε-δ 语言、确界 sup/inf、实数连续性六定理                │
   │  ─ 一致连续、一致收敛(级数+含参积分)                      │
   │  ─ Taylor 公式(Peano + Lagrange 余项)的不等式应用         │
   │  ─ 积分不等式、变上限积分、含参积分一致收敛判别            │
   │  ─ 多元积分 + Green/Gauss/Stokes 的证明向应用             │
   └────────────────────────────────────────────────────────────┘
                              ↓
   ┌────────────────────────────────────────────────────────────┐
   │ 阶段 ②  高代抽象层(必补,提分关键)                高优先  │
   │  ─ 线性空间(子空间、和、直和、商空间思想)                │
   │  ─ 线性映射(表示矩阵、不变子空间、核与像、秩-零度)        │
   │  ─ 最小多项式 + Cayley-Hamilton + 可对角化判据             │
   │  ─ Jordan 标准形(几何重数 vs 代数重数必懂)                │
   │  ─ 内积空间(酉矩阵、Hermite、正规矩阵、谱定理)            │
   │  ─ 二次型(合同、标准形、Sylvester 正定判别)               │
   │  ─ 多项式(Eisenstein、有理根、Vieta、整除)                │
   └────────────────────────────────────────────────────────────┘
                              ↓
   ┌────────────────────────────────────────────────────────────┐
   │ 阶段 ③  解几专题(性价比优先)                       中优先 │
   │  ─ 二次曲面标准形与分类、坐标变换、不变量                  │
   │  ─ 切平面 / 切锥面 / 法线 / 截面(最高频)                  │
   │  ─ 旋转曲面、直纹面(单叶双曲面 / 双曲抛物面)             │
   │  ─ 仿射与正交变换、主轴化(与高代二次型对接)              │
   └────────────────────────────────────────────────────────────┘
                              ↓
   ┌────────────────────────────────────────────────────────────┐
   │ 阶段 ④  解几深化(学有余力,非必要)                  低优先 │
   │  ─ 脐点、第一第二基本形式、主曲率                          │
   │  ─ 曲率 κ、挠率 τ、Frenet 公式                              │
   └────────────────────────────────────────────────────────────┘
                              ↓
   ┌────────────────────────────────────────────────────────────┐
   │ 阶段 ⑤  真题 + 错题复盘                              持续  │
   │  ─ 第 11-17 届真题严格计时,订正时把错题映射回上面四阶段   │
   │  ─ 归类"概念缺口 / 计算失误 / 方法不会 / 时间分配"         │
   └────────────────────────────────────────────────────────────┘
```

### 5.3 时间预算(参考)

假设你有 **3-4 个月**集中备考(如更短优先级再压),大致:

```
阶段 ① 数分地基       6~8 周   (大头,占 50%)
阶段 ② 高代抽象层     5~6 周   (35%)
阶段 ③ 解几专题       2~3 周   (15%,性价比)
阶段 ④ 解几深化       1 周     (学有余力)
阶段 ⑤ 真题训练       贯穿后期,至少 5 套计时模拟
```

每周节奏建议:**3 天学概念补短板 + 2 天做相关真题小题 + 1 天计时大题 + 1 天复盘**。
**至少每周完成 1 道完整证明大题**(数分或高代),严格写过程。

### 5.4 关于"做不完"——重新校准目标

你那个直觉"题这么多,做得完吗"是对的,但**问错了对象**:

```
错的问题:  "我能把 6 题都做完吗?"          (基本不能,几乎没人能)
对的问题:  "我能稳定拿多少分?哪些该放弃?"  (这才是策略)
```

校准一下分数预期(参考各省实际):

```
100 分满分,150 分钟
  ─ 几乎没有人拿满分
  ─ 省一等奖门槛常在 50~70 分区间(各省/各届浮动)
  ─ 进决赛也是按省排名,不是按绝对分
```

策略推论:

```
① 拿分优先,不是"会做优先"
   ─ 6 题里你能稳拿的分(选最熟的、分值小的、首小问),先全收割
   ─ 例:每题第(1)小问通常是送分,6 题首小问 = 30+ 分起

② 大题分步给分,过程即分数
   ─ 即使不会做完,把已知→引理→部分推导写清楚,能拿 40-60% 步骤分
   ─ 跳步、不写理由 = 0 分,即使结论对

③ 果断放弃超纲/完全没思路的题
   ─ 留出时间给熟题写完整;不要在 1 题上耗 40 分钟

④ 真题训练时练"3-3 切分"
   ─ 前 90 分钟做 3 道最熟题,力求完整
   ─ 后 60 分钟做另 3 道题的首小问 + 关键步骤
```

### 5.5 对比蓝桥的正确姿势

你说"感觉比蓝桥难",对,但两者**难的维度不同**:

```
                  蓝桥                        数 A
瓶颈在哪          算法设计 + 实现 + 调试      概念广度 + 严格证明 + 计算
"卡住"是什么感觉  知道要干嘛,不会写/不会优化   看不懂题面在问什么
成长曲线          题型重复高,刷题见效快        概念要建网,刷题见效慢
你的优势区间      有                            部分(计算题)
你的劣势区间      有                            概念抽象层
通关心态          多 AC 一道赚一道              多拿一分赚一分
```

> **心态**:别拿蓝桥的"全做完"标准套数 A。数 A 像"在一块大场地里,
> 每个角落都捡一点东西出来"——**捡得稳比跑得快重要**。

### 5.6 一句话收尾

> 数 A 不是不可挑战,但**第一道关不是"难",是"陌生"**。
> Part 0-4 给你的就是从陌生到熟悉的那座桥:**先用词典把黑话破译,
> 再用全景图把考纲钉住,最后用 Part 4 把超纲题剔除**。
> 做完这三步,你会发现卷子突然"可读"了——而**可读**,是动手的第一步。
>
> 接下来按 Part 5.2 的优先级逐块补,每补一块就回去做对应真题验证,
> 这是非数学专业挑战数 A 最实在的路径。祝你好运。

---

## 附:本手册使用方式

1. **第一遍通读**:建立全景,标记"我完全没听过的""我学过一点的不熟的"两类。
2. **遇到真题术语**回查 Part 0.5 的黑话表和 Part 2-3 的概念展开。
3. **Part 4** 是"识别器",看到天书题先来这里判一下要不要跳。
4. **Part 5.2** 是补短板路线,按它走,不要乱跳。
5. 概念清楚后,再去 `真题与答案/` 文件夹计时训练,订正回查本手册。

> 这份手册**不教你解题**,教你**看懂题目 + 知道它在考哪 + 知道自己差什么**。
> 看懂之后,具体的解题方法训练,留给真题和专题练习。

<!-- 文档结束 -->

